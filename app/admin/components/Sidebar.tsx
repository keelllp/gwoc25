/*
import styles from './Sidebar.module.css';
import Link from 'next/link';

const Sidebar = () => {
  return (
    <div className={styles.sidebar}>
      {/* Home Button */ /*
      <Link href="/" className={styles.homeButton}>🏠 Home</Link>

      {/* Sidebar Navigation */ /*
      <nav className={styles.nav}>
        <Link href="/dashboard/orders" className={styles.sidebarBtn}>📜 Manage Orders</Link>
        <Link href="/dashboard/products" className={styles.sidebarBtn}>📦 Products</Link>
        <Link href="/dashboard/payments" className={styles.sidebarBtn}>💰 Payments</Link>
      </nav>

      {/* Logout Button */ /*
      <button className={styles.logoutBtn}>🚪 Logout</button>
    </div>
  );
};

export default Sidebar; */
